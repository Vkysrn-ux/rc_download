services:
  rc_download:
    build: .
    container_name: rc_download
    restart: unless-stopped
    ports:
      - "3002:3000"
    env_file:
      - .env.local
    environment:
      - PORT=3000
      - NODE_ENV=production
